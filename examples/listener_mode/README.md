Point source with constant torque and force inside of a sphere. Measure streamlines, vortex
lines, and velocity field in x-y plane.

```bash
./gen_config.py
skelly_precompute
skelly_sim
./listener_example.py
```
